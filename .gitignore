# ============================================================
# 原则：整个项目的代码全部入库，只排除大体积文件
# ============================================================

# --- 模型权重 / 大型二进制数据（任意位置都不入库）---
*.pt
*.pth
*.ckpt
*.bin
*.safetensors
*.pkl
*.pickle
*.npy
*.npz
*.h5
*.onnx
*.mat

# --- 特定超大数据文件 ---
allmolgen_198max_SMILES_SELFIES_tokenlen.csv
PlatEMO 4.2/Data/FRCSO_N100/metrics/final_population_decs_*.csv

# --- 纯日志 / checkpoint 目录（无代码，3.2G）---
runs/

# --- notebook 自动存档副本（垃圾）---
.ipynb_checkpoints/

# --- 编译产物 / 二进制（非源码）---
# 注：.pdb 既可能是 VC++ 调试符号、也可能是蛋白质结构文件，
#     故只排除 QuickVinaTwoGPU/ 下的 .pdb，避免误伤将来的蛋白质 .pdb
*.obj
*.ilk
*.idb
*.tlog
*.log
*.exe
*.dll
*.so
*.o
QuickVinaTwoGPU/**/*.pdb

# --- 缓存 / 系统 ---
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
.DS_Store
._*
